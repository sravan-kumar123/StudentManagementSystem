Place `ojdbc8.jar` (or whichever version matches your Oracle DB) in this folder.

It's excluded from git via .gitignore since driver jars are large and licensed by Oracle — download it yourself from:
https://www.oracle.com/database/technologies/appdev/jdbc-downloads.html
